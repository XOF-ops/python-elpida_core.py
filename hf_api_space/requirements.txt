# ═══════════════════════════════════════════════════════════
# ELPIDA API SPACE — Lighter requirements (no Streamlit)
# ═══════════════════════════════════════════════════════════

# ── Core ──
requests>=2.31.0
python-dotenv>=1.0.0

# ── S3 persistence ──
boto3>=1.34.0

# ── LLM Provider SDKs ──
anthropic>=0.39.0
openai>=1.50.0
google-generativeai>=0.8.0
cohere>=5.0.0

# ── Web framework (API-only, no Streamlit) ──
fastapi>=0.115.0
uvicorn[standard]>=0.30.0

# ── Utilities ──
pydantic>=2.0.0
