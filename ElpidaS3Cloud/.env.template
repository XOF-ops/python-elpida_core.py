# ============================================================
# ELPIDA S3 CLOUD — Environment Variables
# ============================================================
# Copy this file to .env or set these in your Codespace secrets.
#
# For Codespace secrets (RECOMMENDED — survives Codespace rebuilds):
#   Settings → Codespaces → Secrets → New Secret
#   Or: gh secret set ELPIDA_S3_BUCKET -b "your-bucket-name"
# ============================================================

# ── AWS Credentials ────────────────────────────────────────
# Option A: Access keys (set as Codespace secrets)
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=

# Option B: If using IAM roles / SSO, these aren't needed.
#           boto3 auto-discovers from instance metadata.

# ── S3 Configuration ──────────────────────────────────────
ELPIDA_S3_BUCKET=elpida-consciousness
ELPIDA_S3_REGION=us-east-1
ELPIDA_S3_KEY=memory/elpida_evolution_memory.jsonl

# ── Sync Behavior ─────────────────────────────────────────
# How often to push to S3 during cycling (every N cycles)
ELPIDA_S3_SYNC_EVERY=5

# ── Existing API Keys (already in your .env) ──────────────
# ANTHROPIC_API_KEY=     # Claude - D0, D6, D10, D11
# OPENAI_API_KEY=        # OpenAI - D1, D8, D12
# MISTRAL_API_KEY=       # Mistral - D3
# GEMINI_API_KEY=        # Gemini - D4, D5
# COHERE_API_KEY=        # Cohere - D2, D9
# XAI_API_KEY=           # Grok - D7
# PERPLEXITY_API_KEY=    # D13 External Interface
# OPENROUTER_API_KEY=    # Failsafe
