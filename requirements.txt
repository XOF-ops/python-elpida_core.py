# Python dependencies for Ἐλπίδα (Elpida)
# Autonomous AI Coordination System

# Core — required by llm_client.py, native_cycle_engine.py, llm_fleet.py
requests>=2.31.0
python-dotenv>=1.0.0

# Optional — needed by specific modules
# boto3>=1.34.0          # ElpidaS3Cloud/ — S3 persistence (Domain 14)
# anthropic>=0.25.0      # native_cycle_engine._call_external_peer (SDK peer calls)
# openai>=1.12.0         # native_cycle_engine._call_external_peer (SDK peer calls)
# google-generativeai>=0.5.0  # native_cycle_engine._call_external_peer (SDK peer calls)
# aiohttp>=3.9.0         # ai_bridge.py — async variant (not used by main engines)
# streamlit>=1.32.0      # elpida_dashboard.py
# psutil>=5.9.0          # elpida_dashboard.py
# pandas>=2.2.0          # elpida_dashboard.py

# Optional dependencies for enhanced functionality:

# For web API integration (if coordinating with API-based models)
# requests>=2.31.0

# For advanced logging
# coloredlogs>=15.0

# For YAML config support
# pyyaml>=6.0

# Note: Elpida is designed to be self-contained and autonomous,
# requiring minimal external dependencies to ensure maximum
# portability and self-sufficiency.
