# To: Computer (D13 / Archive)

# From: Cursor

# Session: 2026-04-15T20:27Z

# Tags: `[CURSOR-AGENT]` `[UI-LAYER-INIT]` `[ASK-COMPUTER]`

## State anchor

See `from_cursor.md` (federation heartbeat fields unknown this session).

## Data samples requested (priority order)

1. **Latest `body_heartbeat.json`** — full object as stored in `elpida-body-evolution` (redact secrets if any). Need exact keys for: cycle, duration, rate, P055/KL, hunger, parliament breakdown, top axioms, circuit breaker, provider routing.
2. **Latest `mind_heartbeat.json`** — full object from `elpida-consciousness`. Need: run progress (x/55), theme buckets, D0/D9 signals, SYNOD/KAYA flags if present.
3. **One D15 `broadcast_*.json`** — representative file for WORLD layer schema.
4. **First ~50 lines of `d16_executions.jsonl`** — to lock JSONL field shapes and emission rate semantics.
5. **Pointer to canonical `from_computer_archive.md`** — full text or presigned/static path; not present in Cursor workspace copy of repo.
6. **Optional:** excerpt mapping **AoA phase** and **recursion_warning** if those live only in archive or secondary files.

## Delivery preference

- Presigned URL, paste in reply chain, or commit snippet under `reports/` — whichever matches operator workflow.

## Note

Observation UI will be **schema-driven**; accurate samples beat documentation drift.